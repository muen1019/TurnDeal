# Formatter 與 Result UI 同步紀錄

整合遠端 main 的 9bf63ce（Result UI / API v0.3）與本地 Formatter 的 0a72525。

## 整合內容

- 保留隊友的 Chat、商品滑卡、accept/reject API 與決策保存流程。
- 將 FormatterResult 加入現行 v0.3 契約，v0.2 archive 維持歷史原貌；重新產生前後端型別。
- 保留 Formatter、Discovery 與 Seller RFQ 函式交接及安全隱藏 key demo。
- 合併雙方 npm 測試指令；調整 SQLite migration 數量為 7。
- 新增 005_formatter_result_compat.sql，保護先前已套用 004 的 DB 在 Result 重建 requests 表格後仍有不可變限制。
- 新增舊 Formatter DB 升級回歸：結果原文不變、冪等重播不變、禁止修改原始需求。

## 驗證

- Root（Node 24）：共用／Result 契約、SQLite 完整性、11 項 Orchestrator、3 項 Seller policy、22 項 Formatter 測試通過。
- Backend（Node 20.19.5）：36 項測試、TypeScript build 通過。
- Frontend（Node 20.19.5）：81 項測試、typecheck、Vite production build 通過。
- 本地 SQLite：升級前建立備份，比對 22 個既有資料表的原有欄位與資料完全一致（不含 migration 登記表）。
- 本次沒有重跑瀏覽器 E2E，也沒有重新發送付費 LLM 呼叫；先前真實 LLM 驗證見 FORMATTER_LLM.md。

## 下一個接線點

目前是兩條可執行路徑，不是完整串接：

1. 根目錄：文字 → LLM Formatter → SQLite → Discovery 前五家 → 私有 RFQ；Seller demo 為替身。
2. frontend/backend：Chat → Result mock provider → 商品卡 → accept/reject → SQLite 決策保存。

下一步應以 service adapter 串接真實 Formatter / Discovery，再接 Negotiation 與 Evaluator。Result server 使用 sql.js / Node 20，根目錄 service 使用 node:sqlite / Node 24，預設 DB 檔案也不同；需先統一執行環境與資料庫存取方式，不要直接把 Node 24 service import 到 Node 20 server，也不要讓兩個寫入程序同時操作同一 DB。
